using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BulletEquipManager : MonoBehaviour
{
    Bullet_Base[] equipBullet=new Bullet_Base[3];
    Case_Base[] equipCase=new Case_Base[3];
    Primer_Base[] equipPrimer=new Primer_Base[3];
    // Start is called before the first frame update
    void Start()
    {
        //セーブデータ読み込み
    }

    // Update is called once per frame
    void Update()
    {
        //記入必要なし？
    }
}
